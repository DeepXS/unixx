const { Client, Collection } = require('discord.js')
const { readdir } = require('fs')

/*
  * Represente um Discord client
  * @extends Discord.Client
*/

class CustomClient extends Client {

  /*
    * @param {Object} options as opc passadas do client
    *
  */
}